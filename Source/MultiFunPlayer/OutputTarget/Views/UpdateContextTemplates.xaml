<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:material="http://materialdesigninxaml.net/winfx/xaml/themes"
                    xmlns:output="clr-namespace:MultiFunPlayer.OutputTarget">
    <DataTemplate DataType="{x:Type output:ThreadPolledUpdateContext}">
        <DockPanel DockPanel.Dock="Left" HorizontalAlignment="Left">
            <TextBlock DockPanel.Dock="Left" Text="错误："/>
            <TextBlock DockPanel.Dock="Right"
                       Width="80"
                       Text="{Binding AverageUpdateError, StringFormat={}{0:F2} ms}"
                       TextAlignment="Right"/>
        </DockPanel>
    </DataTemplate>

    <DataTemplate DataType="{x:Type output:AsyncPolledUpdateContext}">
        <DockPanel DockPanel.Dock="Left" HorizontalAlignment="Left">
            <TextBlock DockPanel.Dock="Left" Text="错误："/>
            <TextBlock DockPanel.Dock="Right"
                       Width="80"
                       Text="{Binding AverageUpdateError, StringFormat={}{0:F2} ms}"
                       TextAlignment="Right"/>
        </DockPanel>
    </DataTemplate>

    <DataTemplate DataType="{x:Type output:ThreadFixedUpdateContext}">
        <StackPanel>
            <DockPanel LastChildFill="True">
                <TextBlock DockPanel.Dock="Left"
                           Margin="0 0 10 0"
                           VerticalAlignment="Center"
                           Text="更新频率："/>
                <TextBlock DockPanel.Dock="Right"
                           Width="40"
                           HorizontalAlignment="Right"
                           VerticalAlignment="Center"
                           Text="{Binding UpdateInterval, Converter={StaticResource MsToHzConverter}, StringFormat={}{0} hz}"
                           TextAlignment="Right"/>
                <Slider DockPanel.Dock="Right"
                        Style="{StaticResource MaterialDesignReversedSlider}"
                        Margin="0 0 10 0"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Top"
                        material:SliderAssist.OnlyShowFocusVisualWhileDragging="True"
                        IsDirectionReversed="True"
                        IsSnapToTickEnabled="True"
                        Maximum="{Binding MaximumUpdateInterval}"
                        Minimum="{Binding MinimumUpdateInterval}"
                        Orientation="Horizontal"
                        TickFrequency="1"
                        Value="{Binding UpdateInterval, Mode=TwoWay}"/>
            </DockPanel>

            <DockPanel Margin="0 0 0 15" LastChildFill="False">
                <DockPanel DockPanel.Dock="Left"
                           Width="100"
                           HorizontalAlignment="Left">
                    <TextBlock DockPanel.Dock="Left" Text="平均："/>
                    <TextBlock DockPanel.Dock="Right"
                               Text="{Binding AverageUpdateRate, StringFormat={}{0} hz}"
                               TextAlignment="Right"/>
                </DockPanel>

                <DockPanel DockPanel.Dock="Right"
                           Width="80"
                           HorizontalAlignment="Left">
                    <TextBlock DockPanel.Dock="Left" Text="抖动："/>
                    <TextBlock DockPanel.Dock="Right"
                               Text="{Binding UpdateRateJitter, StringFormat={}{0} hz}"
                               TextAlignment="Right"/>
                </DockPanel>
            </DockPanel>

            <DockPanel HorizontalAlignment="Left">
                <TextBlock DockPanel.Dock="Left"
                           Margin="0 0 5 0"
                           Text="精确休眠："/>
                <ToggleButton DockPanel.Dock="Left"
                              Style="{StaticResource MaterialDesignSwitchToggleButton}"
                              Margin="0 0 5 0"
                              IsChecked="{Binding UsePreciseSleep}"/>

                <material:ColorZone DockPanel.Dock="Right"
                                    Width="14"
                                    Height="14"
                                    VerticalAlignment="Center"
                                    Background="{DynamicResource PrimaryHueLightBrush}"
                                    CornerRadius="7"
                                    Mode="Custom">
                    <material:ColorZone.ToolTip>
                        <TextBlock>
                            牺牲处理器时间以换取<LineBreak/>
                            更稳定的更新频率（更低的抖动）</TextBlock>
                    </material:ColorZone.ToolTip>
                    <material:PackIcon Width="10"
                                       Height="10"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Foreground="Black"
                                       Kind="QuestionMark"/>
                </material:ColorZone>
            </DockPanel>
        </StackPanel>
    </DataTemplate>

    <DataTemplate DataType="{x:Type output:AsyncFixedUpdateContext}">
        <StackPanel>
            <DockPanel LastChildFill="True">
                <TextBlock DockPanel.Dock="Left"
                           Margin="0 0 10 0"
                           VerticalAlignment="Center"
                           Text="更新频率："/>
                <TextBlock DockPanel.Dock="Right"
                           Width="40"
                           HorizontalAlignment="Right"
                           VerticalAlignment="Center"
                           Text="{Binding UpdateInterval, Converter={StaticResource MsToHzConverter}, StringFormat={}{0} hz}"
                           TextAlignment="Right"/>
                <Slider DockPanel.Dock="Right"
                        Style="{StaticResource MaterialDesignReversedSlider}"
                        Margin="0 0 10 0"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Top"
                        material:SliderAssist.OnlyShowFocusVisualWhileDragging="True"
                        IsDirectionReversed="True"
                        IsSnapToTickEnabled="True"
                        Maximum="{Binding MaximumUpdateInterval}"
                        Minimum="{Binding MinimumUpdateInterval}"
                        Orientation="Horizontal"
                        TickFrequency="1"
                        Value="{Binding UpdateInterval, Mode=TwoWay}"/>
            </DockPanel>

            <DockPanel LastChildFill="False">
                <DockPanel DockPanel.Dock="Left"
                           Width="100"
                           HorizontalAlignment="Left">
                    <TextBlock DockPanel.Dock="Left" Text="平均："/>
                    <TextBlock DockPanel.Dock="Right"
                               Text="{Binding AverageUpdateRate, StringFormat={}{0} hz}"
                               TextAlignment="Right"/>
                </DockPanel>

                <DockPanel DockPanel.Dock="Right"
                           Width="80"
                           HorizontalAlignment="Left">
                    <TextBlock DockPanel.Dock="Left" Text="抖动："/>
                    <TextBlock DockPanel.Dock="Right"
                               Text="{Binding UpdateRateJitter, StringFormat={}{0} hz}"
                               TextAlignment="Right"/>
                </DockPanel>
            </DockPanel>
        </StackPanel>
    </DataTemplate>

    <DataTemplate DataType="{x:Type output:TCodeThreadFixedUpdateContext}">
        <StackPanel>
            <DockPanel LastChildFill="True">
                <TextBlock DockPanel.Dock="Left"
                           Margin="0 0 10 0"
                           VerticalAlignment="Center"
                           Text="更新频率："/>
                <TextBlock DockPanel.Dock="Right"
                           Width="40"
                           HorizontalAlignment="Right"
                           VerticalAlignment="Center"
                           Text="{Binding UpdateInterval, Converter={StaticResource MsToHzConverter}, StringFormat={}{0} hz}"
                           TextAlignment="Right"/>
                <Slider DockPanel.Dock="Right"
                        Style="{StaticResource MaterialDesignReversedSlider}"
                        Margin="0 0 10 0"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Top"
                        material:SliderAssist.OnlyShowFocusVisualWhileDragging="True"
                        IsDirectionReversed="True"
                        IsSnapToTickEnabled="True"
                        Maximum="{Binding MaximumUpdateInterval}"
                        Minimum="{Binding MinimumUpdateInterval}"
                        Orientation="Horizontal"
                        TickFrequency="1"
                        Value="{Binding UpdateInterval, Mode=TwoWay}"/>
            </DockPanel>

            <DockPanel Margin="0 0 0 15" LastChildFill="False">
                <DockPanel DockPanel.Dock="Left"
                           Width="100"
                           HorizontalAlignment="Left">
                    <TextBlock DockPanel.Dock="Left" Text="平均："/>
                    <TextBlock DockPanel.Dock="Right"
                               Text="{Binding AverageUpdateRate, StringFormat={}{0} hz}"
                               TextAlignment="Right"/>
                </DockPanel>

                <DockPanel DockPanel.Dock="Right"
                           Width="80"
                           HorizontalAlignment="Left">
                    <TextBlock DockPanel.Dock="Left" Text="抖动："/>
                    <TextBlock DockPanel.Dock="Right"
                               Text="{Binding UpdateRateJitter, StringFormat={}{0} hz}"
                               TextAlignment="Right"/>
                </DockPanel>
            </DockPanel>

            <DockPanel HorizontalAlignment="Left">
                <TextBlock DockPanel.Dock="Left"
                           Margin="0 0 5 0"
                           Text="精确休眠："/>
                <ToggleButton DockPanel.Dock="Left"
                              Style="{StaticResource MaterialDesignSwitchToggleButton}"
                              Margin="0 0 5 0"
                              IsChecked="{Binding UsePreciseSleep}"/>

                <material:ColorZone DockPanel.Dock="Right"
                                    Width="14"
                                    Height="14"
                                    VerticalAlignment="Center"
                                    Background="{DynamicResource PrimaryHueLightBrush}"
                                    CornerRadius="7"
                                    Mode="Custom">
                    <material:ColorZone.ToolTip>
                        <TextBlock>
                            牺牲处理器时间以换取<LineBreak/>
                            更稳定的更新频率（更低的抖动）</TextBlock>
                    </material:ColorZone.ToolTip>
                    <material:PackIcon Width="10"
                                       Height="10"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Foreground="Black"
                                       Kind="QuestionMark"/>
                </material:ColorZone>
            </DockPanel>

            <DockPanel HorizontalAlignment="Left">
                <TextBlock DockPanel.Dock="Left"
                           Margin="0 0 5 0"
                           Text="间隔交由设备推算："/>
                <ToggleButton DockPanel.Dock="Left"
                              Style="{StaticResource MaterialDesignSwitchToggleButton}"
                              Margin="0 0 5 0"
                              IsChecked="{Binding OffloadElapsedTime}"/>

                <material:ColorZone DockPanel.Dock="Right"
                                    Width="14"
                                    Height="14"
                                    VerticalAlignment="Center"
                                    Background="{DynamicResource PrimaryHueLightBrush}"
                                    CornerRadius="7"
                                    Mode="Custom">
                    <material:ColorZone.ToolTip>
                        <TextBlock>
                            启用时：由设备自行推算每次更新的间隔<LineBreak/>
                            禁用时：由 MultiFunPlayer 计算并发送间隔</TextBlock>
                    </material:ColorZone.ToolTip>
                    <material:PackIcon Width="10"
                                       Height="10"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Foreground="Black"
                                       Kind="QuestionMark"/>
                </material:ColorZone>
            </DockPanel>

            <DockPanel HorizontalAlignment="Left">
                <TextBlock DockPanel.Dock="Left"
                           Margin="0 0 5 0"
                           Text="仅发送变化的轴："/>
                <ToggleButton DockPanel.Dock="Left"
                              Style="{StaticResource MaterialDesignSwitchToggleButton}"
                              Margin="0 0 5 0"
                              IsChecked="{Binding SendDirtyValuesOnly}"/>

                <material:ColorZone DockPanel.Dock="Right"
                                    Width="14"
                                    Height="14"
                                    VerticalAlignment="Center"
                                    Background="{DynamicResource PrimaryHueLightBrush}"
                                    CornerRadius="7"
                                    Mode="Custom">
                    <material:ColorZone.ToolTip>
                        <TextBlock>
                            启用时：每次更新只发送数值有变化的轴<LineBreak/>
                            禁用时：每次更新发送所有轴</TextBlock>
                    </material:ColorZone.ToolTip>
                    <material:PackIcon Width="10"
                                       Height="10"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Foreground="Black"
                                       Kind="QuestionMark"/>
                </material:ColorZone>
            </DockPanel>
        </StackPanel>
    </DataTemplate>

    <DataTemplate DataType="{x:Type output:TCodeAsyncFixedUpdateContext}">
        <StackPanel>
            <DockPanel LastChildFill="True">
                <TextBlock DockPanel.Dock="Left"
                           Margin="0 0 10 0"
                           VerticalAlignment="Center"
                           Text="更新频率："/>
                <TextBlock DockPanel.Dock="Right"
                           Width="40"
                           HorizontalAlignment="Right"
                           VerticalAlignment="Center"
                           Text="{Binding UpdateInterval, Converter={StaticResource MsToHzConverter}, StringFormat={}{0} hz}"
                           TextAlignment="Right"/>
                <Slider DockPanel.Dock="Right"
                        Style="{StaticResource MaterialDesignReversedSlider}"
                        Margin="0 0 10 0"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Top"
                        material:SliderAssist.OnlyShowFocusVisualWhileDragging="True"
                        IsDirectionReversed="True"
                        IsSnapToTickEnabled="True"
                        Maximum="{Binding MaximumUpdateInterval}"
                        Minimum="{Binding MinimumUpdateInterval}"
                        Orientation="Horizontal"
                        TickFrequency="1"
                        Value="{Binding UpdateInterval, Mode=TwoWay}"/>
            </DockPanel>

            <DockPanel Margin="0 0 0 15" LastChildFill="False">
                <DockPanel DockPanel.Dock="Left"
                           Width="100"
                           HorizontalAlignment="Left">
                    <TextBlock DockPanel.Dock="Left" Text="平均："/>
                    <TextBlock DockPanel.Dock="Right"
                               Text="{Binding AverageUpdateRate, StringFormat={}{0} hz}"
                               TextAlignment="Right"/>
                </DockPanel>

                <DockPanel DockPanel.Dock="Right"
                           Width="80"
                           HorizontalAlignment="Left">
                    <TextBlock DockPanel.Dock="Left" Text="抖动："/>
                    <TextBlock DockPanel.Dock="Right"
                               Text="{Binding UpdateRateJitter, StringFormat={}{0} hz}"
                               TextAlignment="Right"/>
                </DockPanel>
            </DockPanel>

            <DockPanel HorizontalAlignment="Left">
                <TextBlock DockPanel.Dock="Left"
                           Margin="0 0 5 0"
                           Text="间隔交由设备推算："/>
                <ToggleButton DockPanel.Dock="Left"
                              Style="{StaticResource MaterialDesignSwitchToggleButton}"
                              Margin="0 0 5 0"
                              IsChecked="{Binding OffloadElapsedTime}"/>

                <material:ColorZone DockPanel.Dock="Right"
                                    Width="14"
                                    Height="14"
                                    VerticalAlignment="Center"
                                    Background="{DynamicResource PrimaryHueLightBrush}"
                                    CornerRadius="7"
                                    Mode="Custom">
                    <material:ColorZone.ToolTip>
                        <TextBlock>
                            启用时：由设备自行推算每次更新的间隔<LineBreak/>
                            禁用时：由 MultiFunPlayer 计算并发送间隔</TextBlock>
                    </material:ColorZone.ToolTip>
                    <material:PackIcon Width="10"
                                       Height="10"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Foreground="Black"
                                       Kind="QuestionMark"/>
                </material:ColorZone>
            </DockPanel>

            <DockPanel HorizontalAlignment="Left">
                <TextBlock DockPanel.Dock="Left"
                           Margin="0 0 5 0"
                           Text="仅发送变化的轴："/>
                <ToggleButton DockPanel.Dock="Left"
                              Style="{StaticResource MaterialDesignSwitchToggleButton}"
                              Margin="0 0 5 0"
                              IsChecked="{Binding SendDirtyValuesOnly}"/>

                <material:ColorZone DockPanel.Dock="Right"
                                    Width="14"
                                    Height="14"
                                    VerticalAlignment="Center"
                                    Background="{DynamicResource PrimaryHueLightBrush}"
                                    CornerRadius="7"
                                    Mode="Custom">
                    <material:ColorZone.ToolTip>
                        <TextBlock>
                            启用时：每次更新只发送数值有变化的轴<LineBreak/>
                            禁用时：每次更新发送所有轴</TextBlock>
                    </material:ColorZone.ToolTip>
                    <material:PackIcon Width="10"
                                       Height="10"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Foreground="Black"
                                       Kind="QuestionMark"/>
                </material:ColorZone>
            </DockPanel>
        </StackPanel>
    </DataTemplate>
</ResourceDictionary>